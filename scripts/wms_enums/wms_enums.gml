enum GLOBAL_WINDOW_PARAM {
	BORDER_DELTA = 32
}

enum WINDOW_SURFACES {
	MAIN_SURFACE,
	WORKFIELD_SURFACE,
	SINGLE_OBJECT_SURFACE
}

enum BUTTON_DRAW_MODE_TYPE {
	DEF,
	CURSORED,
	ACTIVATED,
	PRESSED
}

enum DRAW_MODE_TYPE { 
	//it will probably repeat button and ebox draw mode types
	//however it makes more sense
	DEF,
	CURSORED,
	ACTIVATED,
	PRESSED
}

enum DRAW_PATTERN {
	EBOX,
	BUTTON
}

enum WMS_OBJECT {
	BUTTON
}
